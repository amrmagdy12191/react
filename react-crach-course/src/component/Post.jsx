const names = ['Amr', 'Soad'];
function Post(){
    const name = Math.random = 0.0?names[0]: names[1]; 
    return (
        <div>
            <p>{name}</p>
            <p>React.js is awesome!</p>
      </div>
    );
}
export default Post;